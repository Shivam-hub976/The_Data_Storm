1. "Why did my mobile hotspot block the MongoDB connection in the first place?"
2. "What exactly is 8.8.8.8 and why did adding dns.setServers() fix my ECONNREFUSED error?"
3. "If I change the DNS in Node.js, will it affect my laptop's normal internet connection?"
4. "What does require('dotenv').config() do?
5. "Why do we need app.use(express.json())? Doesn't the server already know I'm sending JSON from Thunder Client?"
6. "How does app.js know about my routes in the other folder? Explain how require() links files together."
7. "What is an ODM and why can't we just save regular JavaScript objects directly to MongoDB?"
8. "Instead of writing out createdAt, why did we use { timestamps: true } in the User model?"
9. "Why can't I just use type: String for the authorId? What is mongoose.Schema.Types.ObjectId?"
10. "What does ref: 'User' do? If I misspell it as ref: 'users', will my code break?
11. "What is the difference between a 201 Created status and a 200 OK status?"
12. "Why does Post.find({}) use empty curly braces? How would I find a post with a specific title instead?"
13. "How does req.params.id pull the ID out of the Thunder Client URL?"
14. "If I try to delete a post that was already deleted, why does findByIdAndDelete return null instead of crashing?"
15. "When I hit POST in Thunder Client, why did the response only show a random 24-character string for the authorId instead of the user's name?"
16. "How exactly does .populate('authorId', 'name email') work behind the scenes?"
17. "What does the second argument 'name email' do inside the populate function? What happens if I remove it?"
18. "If I don't send an authorId when creating a post, why does Mongoose block it?"
19. "Can I use .populate() on a standard string field, or does it only work on ObjectIds?"
20. "Why did my server crash with a 'Cast to ObjectId failed' error when I put the /top/recent route below the /:id delete route?"
21. "How does Express read files? Does it read them top-to-bottom?"
22. "What does -1 mean in .sort({ createdAt: -1 })? How would I sort it from oldest to newest instead?"
23. "Why do we use .limit(3) in the backend instead of just sending all 100 posts and letting the frontend cut off the first 3?"
24. "Can I chain .populate() and .sort() together on any query, or only specific ones?"
25. "Why do we need the path module at the top of app.js for the frontend?"
26. "What does app.use(express.static(...)) actually do? Does it turn my API into a normal website?"
27. "If I add my .env variables to Render, can someone who visits my website steal my database password?"
28. "What is the difference between 'Environment Variables' and 'Secret Files' on Render?"
